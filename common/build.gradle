apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.0"
    publishNonDefault true

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 25
    }
}